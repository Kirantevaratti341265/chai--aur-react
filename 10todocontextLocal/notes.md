create contents folder and todocontext js file in it two thing create and use contexts 
If you are using userContext then must give one context to it like todo or login context etc.
all are arrays every todo is array and it must have id if it has not database.
How many functions are there- add todo(task), toggle(compl uncomlet), edit(each todo task), delete(), read(listing of tasks)


Index.js is create because all filers are imported here and from here export to needed file

First function is creating todos- 